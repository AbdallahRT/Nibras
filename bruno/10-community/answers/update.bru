meta {
  name: Update Answer
  type: http
  seq: 5
}

patch {
  url: {{communityBaseUrl}}/answers/{{answerId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "body": "Updated: BFS complexity depends on the data structure used."
  }
}
