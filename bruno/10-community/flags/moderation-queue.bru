meta {
  name: Moderation Queue
  type: http
  seq: 2
}

get {
  url: {{communityBaseUrl}}/reports/moderation/queue?status=pending&page=1&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
