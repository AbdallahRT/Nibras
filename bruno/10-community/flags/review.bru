meta {
  name: Review Flag
  type: http
  seq: 4
}

patch {
  url: {{communityBaseUrl}}/reports/{{flagId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "action": "dismiss"
  }
}
